class Plants {
  //atribut
  String? name;
  double? height;
  String? color;

  Plants(this.name, this.height, this.color);

  // Constructor
  /**
   * Mmebuat inisialisasi lebih mudah dari pada satu per-satu
   * bisa juga di sebut sebagai method khusus
   * karena berbeda dengan method biasa
   * 1. nama harus sama dengan class
   * 2. tidak memiliki return type
   * 3. dipanggil otomatis ketika objek dibuat
   * 4. digunakan untuk inisialisasi objek
   */
}

